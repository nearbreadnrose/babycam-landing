# 다음 단계 (지금 바로 하세요!)

## ✅ 완료된 것
- [x] Google Cloud 프로젝트 생성 (babycam-landing)
- [x] Google Sheets 생성

## 🔄 지금 해야 할 것

### 1. Google Sheets API 활성화 (10초)
브라우저에서 열린 페이지에서:
- "사용" 버튼 클릭
- 완료되면 다음 단계로

### 2. 서비스 계정 생성 (1분)
다른 브라우저 탭에서:
- "+ 사용자 인증 정보 만들기" 클릭
- "서비스 계정" 선택
- 이름: `babycam-sheets`
- "만들기" → "완료"

### 3. 키 다운로드 (30초)
- 방금 만든 서비스 계정 클릭
- "키" 탭 → "키 추가" → "새 키 만들기"
- JSON 선택 → "만들기"
- 다운로드된 파일을 프로젝트 폴더에 저장

### 4. Google Sheets 공유
- Google Sheets 열기
- "공유" 버튼
- 다운로드한 JSON 파일의 `client_email` 값 입력
- 권한: "편집자" → "전송"

완료되면 알려주세요!

